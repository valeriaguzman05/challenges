/* let roma = ["R", "O", "M", "A"];

roma.forEach(roma => {
    for(i = 0 ; i < roma.length ; i++) {
        if (roma[i] > roma[i+1]) {

        }
    }
}); 

console.log(roma[1+1]) */


array1 = [ 'A', 'M', 'O', 'R' ]
array2 = [ 'A', 'M', 'O', 'R' ]
for ( i = 0 ; i < array1.length ; i++) {
    array1[i] === array2[i];
    console.log(true)
}

